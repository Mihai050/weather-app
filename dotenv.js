
const weatherApiKey = "";
const unsplashApiKey = "";
const googleApiKey = "";

/* 


weatherApiKey = free, used for fetching data about weather

unsplash api = free, used for fetching images about cities

googleApiKey = requires card verification on cloud console, essential for the site to work because it's used for geocoding. 
If it is unavailable use openweather for geocoding instead. (city name to lat + long).

*/
